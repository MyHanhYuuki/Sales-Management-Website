@model IEnumerable<Common.ViewModels.PhieuBaoHanhViewModel>
@{
    ViewBag.Title = "Huỷ phiếu bảo hành";
    Layout = "~/Areas/Admin/Views/Layout/HomeLayout.cshtml";
}
<section id="page-content">
    <div class="header-content">
        <h2><i class="fa fa-list-alt"></i>Phiếu Bảo Hành</h2>
        <div class="breadcrumb-wrapper hidden-xs">
            @using (Html.BeginForm("Confirms", "BaoHanh"))
            {
                <button class="btn btn-sm btn-success btn-xs btn-push" id="submit">
                    <i class="fa fa-check"></i> Xác Nhận Đã Bảo Hành Xong
                </button>
                <input value="" type="hidden" id="id" name="id" class="form-control input-sm" />
            }
            @*<button class="btn btn-lg btn-success btn-xs btn-push" onclick="@("window.location.href='" + @Url.Action("Index", "BaoHanh") + "'"); ">
                <i class="glyphicon glyphicon-arrow-left"></i> Quay lại
            </button>*@
        </div>
    </div>
    <div class="body-content animated fadeIn">
        <div class="row">
            <div class="alert alert-danger1 fade in">
                <span><i class="fa fa-bullhorn"></i>Bạn có chắc chắn muốn xác nhận?</span>
            </div>
            @{Html.RenderAction("ThongTinPhieuBaoHanh", "BaoHanh");}
        </div>
    </div>
</section>
<script>$("#submit").on('click keyup input propertychange paste change', function () {
        var str1 = document.getElementById("soPhieuBaoHanh").value;
        $('#id').val(str1);
    });</script>